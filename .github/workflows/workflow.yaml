name: CI - Workflow
on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
jobs:
  install-test-coverage:
    runs-on: ubuntu-latest
    steps:
      - name: ls
        run: ls -la   
      - name: Checkout
        uses: actions/checkout@v4
      - name: ls
        run: ls -la
